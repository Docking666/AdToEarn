# AdToEarn WebUI — 需求说明文档（v3.2）

> 状态：待评审 | 目标版本：v3.2 | 关联模块：全站
> 本文档用于评审与排期，仅描述需求，不含实现细节。

---

## 1. 背景与问题

当前系统的 AI / 视频 / 采集功能在**未配置 API 或调用失败时自动降级为模拟（mock）数据**，带来两个问题：

| # | 问题 | 影响 |
|---|------|------|
| P1 | **mock 模式掩盖真实配置状态** | 未配置 API 时功能"看似可用"，实际从未发起真实请求；本地与在线运行时均无法判断配置是否正确、请求是否真正发出 |
| P2 | **运行过程不透明** | LLM 调用时无法看到请求/响应上下文（模型、提示词、耗时、错误），排障只能靠猜 |

---

## 2. 改动 A：移除 / 默认关闭 Mock 模式

### 2.1 需求描述

- **默认关闭 mock**：所有 AI、视频生成、数据采集功能在未配置 API 时，**直接返回明确的未配置/失败错误**，不再静默返回模拟数据。
- **可选保留**：mock 仅作为**显式调试开关**存在（需在配置中手动开启），且开启时所有输出必须带醒目的「模拟数据」标识。
- **配置状态可见**：功能入口处明确展示当前配置状态（已配置 / 未配置 / 配置异常），引导用户进入「API 配置」页补齐。

### 2.2 背景说明

mock 数据最初用于"无密钥也能体验流程"，但副作用是：本地与在线运行时，用户**无法区分真实调用与模拟结果**，导致：

- 配置了错误密钥，界面仍"正常"出结果，误判为已上线；
- 真实链路从未被触发，问题（鉴权、端点、模型名）直到上线才暴露。

### 2.3 预期效果

- 未配置 → 操作报错并给出**配置引导**（如"未配置 LLM API，请前往「API 配置」填写密钥"）；
- 配置错误 → 直接暴露真实错误信息（鉴权失败 / 端点不可达 / 模型不存在）；
- 本地与在线行为一致，**所见即真实**；
- 开启调试 mock 时，界面醒目标注「模拟数据」，避免误用。

### 2.4 涉及范围

- 素材反向解析（reverse_parser）：未配置 LLM 时返回未配置错误
- 素材生成（generator 创意方案 + 视频）：未配置对应 API 时返回未配置错误
- 数据采集（scraper）：采集失败返回真实失败原因，不再伪造关键词/素材
- API 配置页：新增「当前配置状态」概览

### 2.5 风险与说明

- 移除 mock 后，**首次体验需要先配置 API**，上手门槛略增 → 由配置状态提示与引导文案弥补。
- 保留调试开关时，需在 `spec.yaml` 增加 `debug.mock_enabled: false`（默认关）。

---

## 3. 改动 B：悬浮运行日志窗口

### 3.1 需求描述

新增**可悬浮展示运行日志的窗口**，实时查看运行上下文，包括但不限于：

- **LLM 调用日志**：模型、Base URL、提示词摘要、返回摘要、Token 用量、耗时、错误；
- **视频生成日志**：任务创建、轮询进度、完成/失败；
- **数据采集日志**：采集状态、命中数量、失败原因；
- **系统事件**：服务启动、API 配置变更、降级提示。

窗口交互要求：

- **可悬浮**：默认悬浮于页面右下角，可拖拽移动、最小化/展开、一键清空；
- **实时推送**：日志通过流式通道实时到达，无需刷新页面；
- **分级过滤**：按 Info / Warn / Error 级别过滤；
- **可见性**：悬浮窗常驻于任意页面，方便在操作时同步观察运行过程。

### 3.2 背景说明

当前运行过程完全不透明：LLM 调用是否成功、用了哪个模型、传了什么提示词、耗时多久，均无可见反馈。遇到"生成为空 / 报错 / 结果异常"时无法定位是配置问题、模型问题还是内容问题。悬浮窗让运行上下文**随操作实时可见**，降低排障成本。

### 3.3 预期效果

- 操作与日志**同屏可见**：提交生成时能实时看到 LLM 请求/响应与耗时；
- 配置异常**即时暴露**：密钥错误、端点不可达等在日志中直接显示原因；
- 排障时间显著缩短，且可作为交付/演示时的运行佐证。

### 3.4 涉及范围

- 后端：新增**日志收集器**（环形缓冲，保留最近 N 条）+ 流式推送端点
- 前端：新增**悬浮日志组件**（拖拽 / 过滤 / 清空 / 最小化）
- 日志埋点：ai_client（LLM 调用）、video 任务、scraper、api_config

### 3.5 风险与说明

- 日志可能包含**提示词/返回内容**（业务敏感）→ 默认仅记录摘要（截断到 N 字符），完整内容需显式开启；
- 悬浮窗需避免遮挡操作区 → 默认收起为小圆点，展开后停靠边缘。

---

## 4. 优先级与排期建议

| 优先级 | 改动 | 建议排期 | 依赖 |
|--------|------|----------|------|
| P0 | A1：默认关闭 mock，未配置直接报错 | 首周 | 无 |
| P0 | B1：日志收集器 + LLM/视频/采集埋点 | 首周 | 无 |
| P1 | B2：悬浮窗组件（拖拽/过滤/清空） | 次周 | B1 |
| P1 | A2：配置状态概览 + 调试开关 | 次周 | A1 |

---

## 5. 验收标准

- [ ] 未配置 API 时，所有功能返回明确错误与配置引导，无任何模拟数据输出（除非显式开启调试 mock，且带「模拟数据」标识）
- [ ] 配置错误时（错误密钥 / 不可达端点），错误信息在结果区与悬浮日志中同步可见
- [ ] 悬浮日志窗可在任意页面拖拽、最小化、按级别过滤、清空
- [ ] LLM 调用日志包含：模型、端点、提示词摘要、返回摘要、耗时、错误
